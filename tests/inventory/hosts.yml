---
all:
  children:
    local:
      hosts:
        molecule-local-instance-1-install-logstash:
    java:
      hosts:
        molecule-local-instance-1-install-logstash:
    elasticsearch:
      hosts:
        molecule-local-instance-1-install-logstash:
