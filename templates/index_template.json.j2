{
  "index_patterns" : ["{{ pipeline.name }}-pipeline"],
  "template": {
    "settings" : {
      "number_of_shards" : {{ install_logstash__elasticsearch_shard }},
      "number_of_replicas" : {{ install_logstash__elasticsearch_shard }}
    }
  }
}